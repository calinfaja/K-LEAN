# K-LEAN LiteLLM Configuration
# ============================
#
# Models are added automatically via `kln provider add <provider>`.
# No quotes around os.environ/ values!

litellm_settings:
  drop_params: true

model_list: []
